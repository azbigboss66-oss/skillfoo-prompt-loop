# SkillFoo Prompt Loop - Environment Variables
# Each project created by `skillfoo init` has its own .env file.
# Never commit a real key.

# API key for the configured OpenAI-compatible provider
SKILLFOO_API_KEY=your_api_key_here
